// import * as CryptoJS from 'crypto-js';
// import * as utf8 from 'crypto-js/enc-utf8';

// export const encryptWithSecret = (str: string, secretKey: string) => {
//   return CryptoJS.AES.encrypt(str, secretKey).toString();
// }

// export const decryptWithSecret = (str: string, secretKey: string) => {
//   return CryptoJS.AES.decrypt(str, secretKey).toString(utf8);
// }

